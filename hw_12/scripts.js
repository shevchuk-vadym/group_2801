var Car = function(x, y) {
  this.x = x;
  this.y = y;
};
const body = document.querySelector("body");

var drawCar = function(Car) {
  var carHtml = document.createElement("div");
  carHtml.className("style");
};
let carElement = drawCar;

// carElement.css({
//   position: "absolute",
//   left: Car.x,
//   top: Car.y
// });
