import { toString, totalSum, mapArray, findMax } from "./functions.js";

// const str = toString(["hello", "world"]);
// console.log(str);

const cartTotal = totalSum([10, 20, 40, 100, "os"]);
console.log(cartTotal);

const mapaArray = mapArray(["somethink", "hello", "world"]);
console.log(mapaArray);

const finderMax = findMax([10, 30, 20, 100, 1992, 1994]);
console.log(finderMax);
